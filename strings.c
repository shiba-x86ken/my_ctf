#include <stdio.h>

int main() {

    int num = 0;

    if (num == 0) {
        printf("Retry...");
    } else {
        printf("flag is {You_understand_how_to_use_strings_command}");
    }

    return 0;
}
